# node
node_modules/
package-lock.json

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.coverage

# test runs
output/
*.pdf
*.svg
*.png
!tests/expected/*.svg
!tests/expected/*.png

# non-root user runs
home
.cache
.config
core

# c4norm: salidas generadas (los crudos de entrada sí se versionan)
tests/fixtures/out_*.drawio.xml

# secrets — nunca versionar
.env

# coordinación de dos loops (ADR-007) — locks efímeros por rol, jamás se commitean
.meta.lock
.loop.lock

# repository
patch.diff
package.json
# ...pero sí versionamos el manifiesto del puente ELK (sus node_modules ya están ignorados)
!c4norm/layout/package.json
